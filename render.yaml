services:
  - type: web
    name: transaction-api
    env: node
    buildCommand: npm install
    startCommand: node src/app.js
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: PORT
        value: 3000